# config

Configs that live under `~/.config/`. Symlink each subdirectory:

```sh
ln -s ~/Projects/.dotfiles/config/ghostty ~/.config/ghostty
ln -s ~/Projects/.dotfiles/config/mise ~/.config/mise
```
